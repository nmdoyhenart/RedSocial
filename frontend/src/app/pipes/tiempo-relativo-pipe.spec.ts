import { TiempoRelativoPipe } from './tiempo-relativo-pipe';

describe('TiempoRelativoPipe', () => {
  it('create an instance', () => {
    const pipe = new TiempoRelativoPipe();
    expect(pipe).toBeTruthy();
  });
});
