import { ResaltarHover } from './resaltar-hover';

describe('ResaltarHover', () => {
  it('should create an instance', () => {
    const directive = new ResaltarHover();
    expect(directive).toBeTruthy();
  });
});
