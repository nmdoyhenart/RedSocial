import { SoloAdmin } from './solo-admin';

describe('SoloAdmin', () => {
  it('should create an instance', () => {
    const directive = new SoloAdmin();
    expect(directive).toBeTruthy();
  });
});
